namespace ZAnalyzers.Core
{
    /// <summary>
    /// Base class for services that should only be registered in DI container without generating HTTP endpoints
    /// 只应在DI容器中注册而不生成HTTP端点的服务的基类
    /// </summary>
    public abstract class FantasyService
    {
    }
}
